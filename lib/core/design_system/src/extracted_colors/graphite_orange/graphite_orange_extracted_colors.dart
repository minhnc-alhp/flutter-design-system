/// Raw color literals extracted from HTML prototypes (Graphite + Warm Orange).
library;

import 'package:flutter/material.dart';

final class GraphiteOrangeExtractedColors {
  const GraphiteOrangeExtractedColors._();

  static const Color primary = Color(0xFFFFAB40);
  static const Color primaryDark = Color(0xFFFF9100);

  static const Color background = Color(0xFF1E1E1E);
  static const Color surface = Color(0xFF2C2C2C);
  static const Color surfaceAlt = Color(0xFF383838);
  static const Color border = Color(0xFF404040);

  static const Color textMain = Color(0xFFFFFFFF);
  static const Color textMuted = Color(0xFFA0A0A0);

  static const Color danger = Color(0xFFFF4545); // one of the pages
  static const Color dangerAlt = Color(0xFFEF4444); // another page
}
